# Datasets

Place labeled YOLO data here (see `training/data.yaml`):

```text
urc_objects/
  images/train/
  images/val/
  images/test/
  labels/train/
  labels/val/
  labels/test/
```

This directory is tracked in git. For very large image sets (>100 MB), consider [Git LFS](https://git-lfs.com).
